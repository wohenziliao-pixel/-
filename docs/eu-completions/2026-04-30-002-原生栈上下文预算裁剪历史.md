# 任务完成记录

- **日期**：2026-04-30
- **序号**：002
- **标题**：修复 EU 原生 ST 聊天栈不裁剪历史导致长对话只出碎片（2T/700T）

## 问题现象

- 目标 700T，实际约 2T，气泡仅「她」等；上下文显示约 21K。
- 非「回退到旧版」，而是**主链路线索**：`EU_ST_NATIVE_CHAT_STACK` 为 true 时，`buildBudgetedMessages` 曾把**整段** `sanitize` 后历史 + 多条 system **全部**进请求，**无 token 预算**，与下方「非原生」分支（按 `max_context` 从**新**往**旧**装历史）行为不一致。长跑剧情下易触发后端/模型不可预期截断，表现为**极短**完成。

## 修改

- **文件**：`public/eu-demo.html`
- 新增 `selectHistoryWithinContextBudget(frontMessages, userText, historyRows, liveProfile)`：用与下方路径同构的 `budget = max_context - max_tokens - 220`，从**最近**一轮往前加入历史，直至超出预算则停止。
- 在 `EU_ST_NATIVE_CHAT_STACK` 分支中，用 `historySlice` 替换原 `safeHistory` 全量 `map(...)`。

## 建议自测

1. 开「解锁」并拉到 ~16K–20K 上下文、长对话题材后，再发一轮；**不应**再仅出现 1 字，除非 API 或模型真如此返回。
2. 打开控制台看 `getEuFeatureProbe()` 与 `window.__EU_LAST_FIRST_PASS__`：`raw` 大、`out` 极短时偏后处理；`raw` 就极短偏上行。

# 2026-05-02-004 会话删除仅允许按 key 精确删除

## 任务

用户明确要求：「不是回滚问题，而是就不该删除」。

## 改动

- `removeConversationRuntime` 改为**严格模式**：
  - 必须传入且命中 `convKey` 才执行删除；
  - 不再允许“按角色名回退匹配后批量删”。
- `confirmDelete` / `confirmDeleteConv` 改为先从 `state.characterSessions` 解析 `convKey`，再打开删除确认弹窗，避免走无 key 路径。
- 保持会话删除入口语义：只删除当前会话，不触碰资源获取状态。

## 路径

- `public/eu-demo.html`
- `EU_CLIENT_BUILD_STAMP = 20260502-031`

# 2026-05-02-002 回退产出基准软约束注入

## 任务

用户反馈「逻辑仍有问题」，要求先回退到“未做约束注入”的版本。

## 回退内容

- 移除 `getOutputBaselineSystemMessage()`（此前会注入“本轮长度偏好”system）。
- 从 `buildBudgetedMessages` 三条路径移除 `outputBaselineSystem` 注入：
  - native stack optionalSystem
  - strict replica strictSystem
  - 非 native 的 sysCore
- 状态条 title 文案去掉“并注入长度偏好提示”描述。

## 保留内容

- 「产出基准」UI 与基准值输入仍保留。
- `max_tokens` 基准上下浮动逻辑仍保留（不恢复旧版多轮补长）。

## 路径

- `public/eu-demo.html`（`EU_CLIENT_BUILD_STAMP` -> `20260502-029`）

# 2026-05-01-023 移除 700 回复目标与 EU 补长整套

## 任务

用户指出「700 补齐」为旧规则，在贴近酒馆思考链 / 严格对齐时应已废弃；要求删除相关整套逻辑，而非仅在 strict 下短路。

## 做了什么

- **删除** `ensureReplyMinTokens` 整函数（多轮续写、字数硬要求、收尾句号兜底等全部移除）。
- **删除** `getActiveRuleMaxReplyTokens` / `getActiveRuleMinReplyTokens`。
- **`requestAiReply`**：移除 `skipMinReplyCheck` 及所有 `ensureReplyMinTokens` 调用；`max_tokens` 仅在传入 `maxReplyTokensOverride` 时覆盖，否则使用 `buildStGeneratePayload` + `fetchStLiveGenerationProfile` 中的酒馆 preset `openai_max_tokens`。
- **`requestAiReplyStream`**：不再用 EU 规则覆盖 `stPayload.max_tokens`。
- **`getEuStGenerationProfile` / `fetchStLiveGenerationProfile`**：去掉 `replyTargetEnabled` / `replyTokens` 分支，兜底 `max_tokens` 固定为与 preset 读取一致的路径。
- **`state.runtimePrefs` / `loadRuntimePrefs`**：不再读写 `replyTargetEnabled`、`replyTokens`（旧 localStorage 字段读入时自然忽略）。
- **设置页**：移除「文本回复量」滑块；保存提示改为说明回复长度在 SillyTavern 预设中调整。
- **「新增规则」弹窗**：移除「启用文本回复量」与 Token 数字框，仅保留每故事书规则文本。
- **`getActiveAuthorRuleConfig` / `setActiveAuthorRuleConfig`**：只保留 `storybookRules`；`conv.metadata.authorRule` 只存规则行。
- **气泡 token 角标**：改为仅展示估算 `≈NT`，不再与「目标 700T」对比打 ✓/!。

## 改动路径

- `public/eu-demo.html`（`EU_CLIENT_BUILD_STAMP` → `20260501-023`）

## 说明

- `public/单机稳定版.html` 等仍保留旧逻辑；若需一致需单独同步（本任务仅改 `eu-demo.html`）。

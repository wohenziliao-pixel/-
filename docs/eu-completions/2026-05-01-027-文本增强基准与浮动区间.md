# 2026-05-01-027 文本增强基准与浮动区间

## 任务

将「max_tokens 固定值覆盖」改为更易操作的「增强文本回复量」：

- 用户填写一个基准值；
- 请求时在该基准上下浮动（区间化），不要求每次精确命中。

## 改动

- 「新增规则」弹窗文案更新：
  - `启用增强文本回复量（覆盖酒馆预设 max_tokens）`
  - `增强文本回复量基准（320~2000）`
- `applyEuReplyMaxTokensToProfile` 从固定覆盖改为区间采样：
  - 基准 = 用户填写值；
  - 每次请求 `max_tokens` 在约 `基准 * [0.88, 1.12]` 范围内随机浮动；
  - 仍受 320~2000 钳制。
- 世界聊天状态条改为显示：
  - `文本增强：基准X（浮动A-B）`
- 对齐说明文案更新为「新增规则中启用文本增强并按基准上下浮动」。

## 路径

- `public/eu-demo.html`（`EU_CLIENT_BUILD_STAMP` 更新为 `20260501-027`）

## 备注

- 仍然是单次 completion 的上限控制；
- 未恢复旧版多轮补长逻辑。

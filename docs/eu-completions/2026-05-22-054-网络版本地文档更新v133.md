# 网络版本地文档更新（v133 发布前）

## 任务

用户要求「准备网络版更新，先更新本地」：同步部署口径与 v131～v133 上线清单，便于本机 commit + push 后服务器 `git pull`。

## 做了什么

- 更新 `docs/EU-部署与入口说明.md` 构建戳 → **v133**
- 新增 `docs/eu-completions/2026-05-22-053-网络版v133同步指令.md`（本机 git / 服务器 pull / 验收）
- 本单记录发布前文档状态

## 改动路径

- `docs/EU-部署与入口说明.md`
- `docs/eu-completions/2026-05-22-053-网络版v133同步指令.md`
- `docs/eu-completions/2026-05-22-054-网络版本地文档更新v133.md`

## 下一步（需你本机执行）

按 `053` 中 PowerShell 执行 `git add` / `commit` / `push`；服务器按 `053` §服务器 操作。

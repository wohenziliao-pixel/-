# 移除示例卡与自动清理设置

## 任务目标

按用户要求：仅处理本次示例卡清理，不保留“无按钮自动处理”这类额外设置。

## 做了什么

- 直接从 `mallData` 移除本次指定的内置示例故事书/设定集：
  - `魔法学院物语`
  - `异世界勇者传`
  - `樱花小镇物语`
  - `科幻设定合集`
  - `中世纪奇幻设定`
- 删除此前新增的自动处理逻辑：
  - `autoHideBuiltinDemoMallItemsOnce` 函数
  - `DOMContentLoaded` 里的自动调用
- 删除与自动处理相关的完成记录文档，避免与现状不一致。

## 改动路径

- `public/eu-demo.html`
- `docs/eu-completions/2026-05-11-009-内置示例卡自动一次性隐藏.md`（删除）

## 用户自测建议

1. 刷新商城页面后，上述示例故事书/设定集不再出现。
2. 代码中不再存在自动清理示例卡的初始化逻辑。

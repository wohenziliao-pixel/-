# 2026-05-01-026 将 max_tokens 开关并入新增规则

## 任务

用户要求：不要单独的设置入口/单项，把这次新增的文本回复上限能力并入「新增规则」弹窗。

## 改动

- 从设置页移除「单次回复 max_tokens（EU 覆盖）」控件。
- 从侧栏移除「设置」导航项；右上用户下拉移除「设置（max_tokens 等）」。
- 在「新增规则」弹窗新增两项：
  - `启用单次回复 max_tokens（覆盖酒馆预设）`
  - `目标 max_tokens（320~2000）`
- `applyAuthorRuleFromInputs` / `setActiveAuthorRuleConfig` 现在同时保存：
  - `storybookRules`
  - `replyMaxTokensOverrideEnabled`
  - `replyMaxTokens`
- 打开「新增规则」时会回填并根据开关禁用/启用数值框。
- 保留设置页原有上下文相关设置，不再承载本次输出上限能力。

## 路径

- `public/eu-demo.html`（`EU_CLIENT_BUILD_STAMP` 更新为 `20260501-026`）

## 备注

- 这次仅改变入口与归属，不恢复任何多轮补长逻辑；仍是单次 `max_tokens` 覆盖。

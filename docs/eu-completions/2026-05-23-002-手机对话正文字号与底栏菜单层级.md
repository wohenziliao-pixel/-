# 手机对话正文字号与底栏菜单层级

## 任务

手机故事书对话：正文字体加大两档；左下 ☰ / 💡 弹出层被主对话区盖住无法操作。

## 原因

- 正文原为 `1.05rem`，手机阅读偏小。
- `#page-world-chat` 为 `z-index: 150`，底栏仅 `90`；☰ 菜单为 `absolute` 且 `z-index: 95`，叠层低于对话全屏层。

## 改动

- 全局故事书气泡：`1.05rem` → `1.25rem`；手机 `@media (max-width:900px)` 再为 `1.3rem`。
- 对话中：`body[data-world-chat-active]` 时底栏 `z-index: 210`；☰ 菜单、帮答面板、作者规则面板改为 `position: fixed` 且 `z-index: 221`。

## 路径

- `public/eu.html`（`20260523-mobile-chat-font-tools-zfix-v141`）

## 自测

手机进故事书：正文明显变大；点 ☰、💡 菜单应浮在对话区上方可点。

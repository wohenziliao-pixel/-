# 网络版 v142 同步指令（2026-05-23 统一包）

## 构建戳

`20260523-shared-api-cm-sync-v142`

| 子版本 | 内容 |
|--------|------|
| v138 | 手机故事书对话顶栏/标题横排/z-index |
| v139 | `messages`→`messagesNs` 笔误；generate 404 备用路径 |
| v140 | `auditEuWritingStyleApis()` 五条文风诊断 |
| v141 | 手机正文字号加大；对话时 ☰/💡 弹层 z-index |
| **v142** | **共用 API 同步 Connection Manager 五连接（其他用户现代风可聊）** |

## 必传文件（Git 或 WinSCP）

| 路径 | 说明 |
|------|------|
| `public/eu.html` | 前端 v138～v142 |
| `src/endpoints/eu-shared-api.js` | 套用共用 API 时合并五条连接 profile |
| `tools/eu-audit-writing-style-apis.mjs` | 可选，服务器抽查用户文风 |

**不必传**：整盘 `data/`、本机 `config.yaml`（线上单独改项见下）。

## 本机

```powershell
cd D:\Eureka\SillyTavern\SillyTavern-1.17.0
git push origin main
```

若 push 卡住 → WinSCP 传上表两个文件到 `/opt/SillyTavern/` 对应路径。

## 服务器（必做）

```bash
cd /opt/SillyTavern
git pull origin main
grep "const EU_CLIENT_BUILD_STAMP =" public/eu.html
# 应含：shared-api-cm-sync-v142
```

### 1. config.yaml（若尚未配置）

```yaml
enableUserAccounts: true
euSharedApiFromHandle: wohenziliao
```

改后 **必须重启 Node**。

### 2. 给所有老用户补连接 + 密钥（v142 核心）

```bash
node tools/eu-shared-api-sync-all.mjs
node tools/eu-audit-writing-style-apis.mjs lynn0426
```

审计五条应均为 OK。

### 3. 重启

```bash
# 按你的进程管理方式，例如：
pm2 restart sillytavern
# 或 systemctl restart sillytavern
```

仅改 `eu.html` 可不重启；**改了 eu-shared-api.js 或 config 必须重启**。

浏览器 **Ctrl+F5**；控制台应见 `[次元姬 EU] client build 20260523-shared-api-cm-sync-v142`。

## 验收

- [ ] 站长 `wohenziliao` 现代风可发消息
- [ ] 其他用户（如 `lynn0426`）重新登录后现代风可发消息，不再报「文风暂时不可用」
- [ ] 手机进故事书：标题横排、开场白不被挡、正文字号偏大、☰/💡 弹层在最上层
- [ ] `await auditEuWritingStyleApis()` 五条 `ok: true`（站长账号）

## 相关完成单

- `2026-05-22-060`～`062`（v138～v140）
- `2026-05-23-002`（v141）
- `2026-05-23-003`（v142 共用 API）

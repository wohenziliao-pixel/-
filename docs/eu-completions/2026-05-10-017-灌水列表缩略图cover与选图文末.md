# 灌水列表缩略图恢复 cover 且图在文下；选图插入改文末

## 需求

- 列表缩略图改回 `object-fit: cover`（裁切铺满）。
- 列表里图不要与摘要左右并排，默认在文字下方。
- 发帖/回复「选图」插入的 `![](路径)` 默认追加到正文末尾，而非光标处（避免落在两段文字之间）。

## 改动

- `public/eu-demo.html`：`.tieba-feed-body-row` 纵向排列；`.tieba-feed-thumb-wrap` 宽 `max-width: min(360px,100%)`、高 120px；`img` 恢复 `object-fit: cover`；移动端仅调缩略区高度。
- `euTiebaInsertMarkdownImageAtEnd` 替代光标插入；`euTiebaPickImageForField` 调用文末追加；相关说明文案与 Toast 更新；`EU_CLIENT_BUILD_STAMP` `20260512-070`。

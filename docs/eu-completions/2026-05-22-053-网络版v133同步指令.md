# 网络版 v133 同步指令（文风 System + 全年龄/18+ + UI）

## 版本

| 项 | 值 |
|----|-----|
| **EU 构建戳** | `20260522-writing-style-toggle-inrow-v133` |
| 相对 v123 | v124～v133（文风密钥轮换、400 修复、流式光标、五档 System 终稿、弹窗全年龄钮） |
| **Node 重启** | 线上若已是 **v119+**：**不必**重启；仅 `git pull` + 用户 **Ctrl+F5** |
| **必传** | `public/eu.html`（主变更） |

## 本版功能摘要（v131～v133）

| 构建 | 内容 |
|------|------|
| v131 | 文风弹窗每档 **全年龄 / 含18+** 切换；`EU_WRITING_STYLE_AGENT_SYSTEM_SFW` |
| v132 | 五档 18+ **深渊欲火** System 终稿覆写（现代/文学/温柔/创意/重口） |
| v133 | 切换钮移入**卡片内右侧**，尺寸对齐手机顶栏 compact |

另含 v124～v130：custom/openrouter/xai 文风密钥轮换、合并 system 修 400、GLM5.1 语法与 secret-id、流式光标与误续写等（见 `043`～`049`）。

## 本机（上传前）

```powershell
cd D:\Eureka\SillyTavern\SillyTavern-1.17.0

# 核对构建戳
Select-String -Path public\eu.html -Pattern "EU_CLIENT_BUILD_STAMP" | Select-Object -First 1
# 应含：writing-style-toggle-inrow-v133

git add public/eu.html
git add docs/EU-部署与入口说明.md
git add docs/eu-completions/2026-05-22-043-*.md
git add docs/eu-completions/2026-05-22-044-*.md
git add docs/eu-completions/2026-05-22-045-*.md
git add docs/eu-completions/2026-05-22-046-*.md
git add docs/eu-completions/2026-05-22-047-*.md
git add docs/eu-completions/2026-05-22-048-*.md
git add docs/eu-completions/2026-05-22-049-*.md
git add docs/eu-completions/2026-05-22-050-*.md
git add docs/eu-completions/2026-05-22-051-*.md
git add docs/eu-completions/2026-05-22-052-*.md
git add docs/eu-completions/2026-05-22-053-*.md
# 勿 add tools/eu-mall-batch.config.json、data/**

git commit -m "EU v133: writing-style SFW toggle, agent prompts final, in-row compact toggle"
git push origin main
```

## 服务器（复制执行）

```bash
cd /opt/SillyTavern
# 或你的实际路径：/path/to/SillyTavern-1.17.0

git pull origin main
grep EU_CLIENT_BUILD_STAMP public/eu.html | head -1
# 应含：writing-style-toggle-inrow-v133
```

| 线上当前 | 操作 |
|----------|------|
| v119 及以上 | **不必**重启 Node |
| v118 或更早 | `git pull` 后 **必须**重启 Node |

```bash
# pm2 示例
pm2 restart sillytavern
```

## 浏览器验收

1. `https://<域名>/eu.html` → F12：`[次元姬 EU] client build … v133`
2. 进故事书 → **文风转换** → 每行**框内右侧**小钮：绿「全年龄」/ 红「含18+」
3. 同一文风：全年龄发一句 vs 含18+ 发一句，尺度应有明显差异（后台 System 不同，界面不展示全文）
4. 五档各发一句：现代/文学/温柔/创意/重口 路由正常，无 400/401（温柔风需 GLM secret-id 已绑定）

## 勿覆盖

- **不要**上传本机 `data/` 覆盖线上用户
- **不要**提交 `tools/eu-mall-batch.config.json`

## 相关完成单

`043`～`052`（400/密钥/流式/v130 模型对调/v131 切换/v132 System/v133 UI）

# 灌水列表缩略图改为等比完整显示

## 问题

主题列表右侧缩略图使用 `object-fit: cover`，固定框内裁切，只能看到原图局部。

## 改动

- `public/eu-demo.html`：`.tieba-feed-thumb-wrap img` 改为 `object-fit: contain` + `object-position: center`，在 148×100（移动端为宽×min 高）框内整图等比缩小，必要时留边；`EU_CLIENT_BUILD_STAMP` `20260512-069`。

## 自测

灌水吧列表含首楼图的主题：缩略图应完整可见、不变形。

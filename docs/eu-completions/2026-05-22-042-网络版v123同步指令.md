# 网络版 v123 同步指令（本机已推 Git 后执行）

## 版本

| 项 | 值 |
|----|-----|
| **EU 构建戳** | `20260522-writing-style-agent-system-v123` |
| 含 v119 | 游客逛商城、获取、10 轮试用（需 Node 重启过） |
| 含 v120～123 | 回复去重/少重写、文风路由校验、错误脱敏、五档隐藏 System Prompt |

## 本机（已完成项）

```powershell
cd D:\Eureka\SillyTavern\SillyTavern-1.17.0
git add public/eu.html docs/eu-completions/2026-05-22-038-*.md docs/eu-completions/2026-05-22-039-*.md docs/eu-completions/2026-05-22-040-*.md docs/eu-completions/2026-05-22-041-*.md docs/eu-completions/2026-05-22-042-*.md docs/EU-部署与入口说明.md
git commit -m "EU v123: writing-style agent prompts, error sanitize, reply fixes"
git push origin main
```

## 服务器（复制执行）

```bash
cd /opt/SillyTavern
git pull origin main
grep EU_CLIENT_BUILD_STAMP public/eu.html | head -1
# 应含：writing-style-agent-system-v123
```

### 是否重启 Node

| 线上当前版本 | 操作 |
|--------------|------|
| 已是 **v119+**（游客接口已可用） | **不必**重启 Node；用户 **Ctrl+F5** 强刷 `eu.html` 即可 |
| 仍是 **v118 或更早** | `git pull` 后 **必须重启** Node（游客商城/试用 API 在 `src/`） |

```bash
# 示例：pm2
pm2 restart sillytavern
# 或手动结束 node 后重新 node server.js
```

### 浏览器验收

1. 打开 `https://<你的域名>/eu.html`，F12 控制台：`[次元姬 EU] client build 20260522-writing-style-agent-system-v123`
2. 温柔风/创意风发消息：错误提示不出现模型名、URL
3. 切换文风发一条：Network → `generate` 的 messages 含「深渊欲火」system（仅运维自查，普通用户界面不展示）

### 勿覆盖

- **不要**上传本机 `data/` 覆盖线上用户目录
- **不要**提交 `tools/eu-mall-batch.config.json`

## 相关完成单

- `038` 回复重复与文风 500 校验  
- `039` 温柔/创意连接核对  
- `040` 用户可见错误脱敏  
- `041` 五档隐藏 System Prompt  

# 移除商城示例卡按钮

## 任务目标

按用户要求移除「清理商城示例卡」入口，避免在开发者页面保留未来不会使用的按钮与逻辑。

## 做了什么

- 删除开发者批量操作区中的 `清理商城示例卡` 按钮。
- 删除对应函数 `cleanupBuiltinDemoMallItems`。
- 移除上一条针对该按钮的完成记录文档，避免文档与当前代码状态不一致。

## 改动路径

- `public/eu-demo.html`
- `docs/eu-completions/2026-05-11-007-商城示例卡一键隐藏.md`（删除）

## 用户自测建议

1. 刷新开发者页面后，批量操作区不再出现该按钮。
2. 其余批量功能（全选/批量删除/清理幽灵故事书）保持可用。

# 底栏精简与新用户 API 批量同步

## 任务
1. 说明新注册用户是否自动套用站长 xAI，及存量用户处理。
2. 底栏从左到右：删第 2、3 个按钮（↻ 重新生成、➤ 续写），删最右发送钮；第 4 个 💡 移到 ☰ 旁；输入框占满剩余宽度。

## 做了什么

### 新用户 / 存量用户 API
- 注册时已有 `applyEuSharedApiProfile`（`eu-auth.js`），部署加强版 `eu-shared-api.js` 后会强制 `xai`。
- 登录 `enterApp` 会调 `POST /api/eu/shared-api/sync`（需部署 `server-main.js` + `eu.html`）。
- 新增 `tools/eu-shared-api-sync-all.mjs`：服务器一次性为 `data/` 下所有非站长目录套用 API。

### 底栏 UI（`public/eu.html`）
- 仅保留：☰ 工具菜单、💡 AI 帮答、输入框（Enter 发送）。
- 移除：`world-continue-btn`；此前已移除 ↻、发送 ▶。
- 续写/重新生成仍在 ☰ 菜单内。
- CSS：`#assistant-input` flex 撑满。

## 改动路径
- `public/eu.html`
- `tools/eu-shared-api-sync-all.mjs`
- （沿用）`src/endpoints/eu-shared-api.js`、`src/server-main.js`

## 部署
本机 `git push` 后服务器 `git pull`，上传/覆盖 `public/eu.html`，重启 node。浏览器强刷或无痕。

服务器批量修存量用户：
```bash
cd /opt/SillyTavern
node tools/eu-shared-api-sync-all.mjs
```

## 自测
- [ ] 底栏仅 ☰、💡 + 宽输入框，无 ↻、➤、蓝发送钮
- [ ] 新注册账号发消息不报 OpenAI key
- [ ] 旧账号跑批量脚本或重新登录后正常

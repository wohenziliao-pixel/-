# 网络版 v137 同步指令（文风 + 帮答 + 开场白中译）

## 构建戳

`20260522-opening-zh-translate-v137`

含：v134 文风弹窗隐藏 API · v135 默认全年龄 · v136 AI 帮答五档随机三路 · v137 开场白英文译中

## 本机

```powershell
cd D:\Eureka\SillyTavern\SillyTavern-1.17.0
git push origin main
```

若 push 失败 → WinSCP 只传 `public\eu.html` 到 `/opt/SillyTavern/public/eu.html`

## 服务器

```bash
cd /opt/SillyTavern
git pull origin main
grep "const EU_CLIENT_BUILD_STAMP =" public/eu.html
# 应含：opening-zh-translate-v137
```

**不必重启 Node**（仅前端）。浏览器 **Ctrl+F5**。

## 验收

- [ ] 文风弹窗：框内全年龄/含18+ 小钮，无 API 行，默认绿全年龄
- [ ] 💡 帮答：3 条中文草稿
- [ ] 英文开场故事书进对话 → 开场以中文为主
- [ ] 控制台 build = v137

# 2026-04-30-012 状态栏显示正则与词条命中

## 任务
- 将“故事书绑定 / 正则开关 / 世界书命中”运行态放到世界聊天顶部状态栏（用户指定位置），便于判断 EU 后台链路是否生效。

## 改动
- `public/eu-demo.html`
  - 新增 `getWorldInfoMatchedEntries(entries, userText)`：复用世界书 key 命中逻辑。
  - 新增 `getWorldInfoMatchStats(userText)`：统计总条目、命中数、实际注入数（上限 3）。
  - `updateWorldChatFeatureStatus()` 扩展展示：
    - `书：已绑/未绑`
    - `正则：开/关`
    - `词条命中：注入数/命中数`
  - 状态栏 `title` 悬浮提示增加明细：绑定书名、正则状态、世界书注入/命中/总条目。

## 影响路径
- `public/eu-demo.html`

## 用户自测
- 进入故事书会话后，顶部状态栏应显示 `书：已绑` 与 `正则：开/关`。
- 发送包含世界书 key 的输入，`词条命中` 数值应变化。
- 鼠标悬停状态栏，确认能看到世界书总条目与注入细节。

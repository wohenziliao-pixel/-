# 底栏三图标并入输入框恢复（v100）

## 任务

恢复此前已验收、但未进 Git 的底栏 UI：

1. 左侧 ☰、💡 **缩小**
2. 悬停 **取消外边框**（全局 `button` 渐变边框）
3. **恢复** 右侧发送 ➤
4. **三个图标均在同一输入框容器内**，与输入区 **上下居中**

## 还原原因（为何被“打回”）

| 阶段 | 说明 |
|------|------|
| 2026-05-18 `8f6ceeb` | 为加宽输入框 **删掉** 底栏发送钮（`010`/`011` 完成单） |
| 2026-05-19 会话 | 在本地实现 v54→v59：`bottom-input-compose` + 框内发送 + 工具钮入框；**未 commit/push** |
| 2026-05-19 晚～20 日 | 对话云 v90–v99 大量改 `eu.html`，**覆盖** 底栏 HTML/CSS |
| Git 证据 | `git log -S bottom-input-compose -- public/eu.html` **无提交** |

往期成功记录见同会话完成单意图（原 `2026-05-19-004`～`009` 仅存在于对话，仓库内无文件）；本次以 v59 结构为准重做并 **入库**。

## 做了什么

### 结构（`public/eu.html`）

```text
.card
  └── .bottom-input-compose（单圆角框）
        ├── .bottom-input-tools → ☰ 💡
        ├── #assistant-input
        └── #assistant-send-btn ➤
```

↻ 重新生成 / 续写仍在 ☰ 菜单，不占底栏位。

### 样式要点

- 工具钮：桌面 28px / 手机 26px，`border/outline/box-shadow: none !important`，悬停仅淡底
- 发送钮：框内右侧，34px，渐变紫，覆盖全局 `button` 样式
- `.bottom-input-compose`：`align-items: center` 三图标与输入区垂直居中
- 覆盖全局 `button, textarea { width:100% }` 避免底栏竖排挤版

### 构建戳

`20260520-bottom-bar-compose-inbox-v100`（保留 v99 对话云逻辑，仅 UI 戳递增）

## 改动路径

- `public/eu.html`

## 部署与自测

1. 本机：`grep bottom-input-compose public/eu.html` 有输出；Console `client build` 含 **v100**
2. 线上：`git push` → `/opt/SillyTavern` → `git pull` → **Ctrl+F5** 或 `eu.html?v=100`（**不必**重启 Node）
3. 验收：底栏为 **单框「☰💡 | 输入… | ➤」**；悬停工具钮 **无外框**；Enter/点击 ➤ 可发送

# 温柔风 / 创意风连接配置核对与 EU 同步

## 任务

用户已在酒馆 Connection Manager 更新 **GLM5.1**（温柔风）、**VENICE**（创意风），要求与旧数据对比是否改动，并保存核对结果。

## 对比（`data/wohenziliao/settings.json` → `connectionManager.profiles`）

### GLM5.1（EU 文风：温柔风）

| 字段 | 旧值 | 新值 | 说明 |
|------|------|------|------|
| `api` | custom | custom | 未变 |
| `api-url` / `custom_url` | `https://open.bigmodel.cn/api/paas/v4/` | 同左 | 未变 |
| `model` / `custom_model` | **`z-ai/glm-5.1`** | **`glm-5.1`** | **已改**：与智谱官方地址匹配 |
| `secret-id` | （profile 未单独列出） | 同左 | 未变 |

**结论**：温柔风已从「OpenRouter 风格模型名 + 智谱地址」修正为「智谱模型名 + 智谱地址」，可正常发请求。

### VENICE（EU 文风：创意风）

| 字段 | 旧值 | 新值 | 说明 |
|------|------|------|------|
| `api` | custom | custom | 未变 |
| `api-url` / `custom_url` | `https://api.venice.ai/api/v1` | 同左 | 未变 |
| `model` / `custom_model` | **`venice-uncensored`** | **`venice-uncensored-role-play`** | **已改**：Venice 角色扮演模型 |
| `secret-id` | `ef279166-a801-4e1f-b4ca-4475db6e897c` | 同左 | 未变 |

**结论**：创意风仅模型 ID 更新；地址与密钥绑定未变。发消息前建议在酒馆对 **VENICE** 点一次 **连接**，确保 Custom API 激活的是 Venice 密钥。

## 代码同步

`public/eu.html` 中 `EU_WRITING_STYLE_PRESETS` 已与上表对齐（避免未读到 profile 时仍用旧 `stModel`）：

- 温柔风：`glm-5.1` + `customUrl` 智谱
- 创意风：`venice-uncensored-role-play` + `customUrl` Venice

构建戳：`20260522-writing-style-glm-venice-sync-v121`

## 改动路径

- `data/wohenziliao/settings.json`（用户已在酒馆保存，本任务仅核对）
- `public/eu.html`
- `docs/eu-completions/2026-05-22-039-温柔创意风连接配置核对与EU同步.md`

## 自测

1. 强刷 `eu.html`，build 为 `v121`。
2. 控制台：`dumpEuWritingStyleApiAudit()`  
   - 温柔：`tavernModel` → `glm-5.1`  
   - 创意：`tavernModel` → `venice-uncensored-role-play`
3. 酒馆 **VENICE → 连接** 后，创意风发一条；温柔风发一条，均不应再出现「z-ai 不能直连智谱」类报错。

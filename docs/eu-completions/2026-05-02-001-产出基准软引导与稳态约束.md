# 2026-05-02-001 产出基准软引导与稳态约束

## 任务

在“可控文字量”的前提下，降低逻辑混乱/文风跑偏风险：把“增强文本回复量”升级为“产出基准”语义，并尽量通过首轮软引导达成，不使用多轮硬补长。

## 改动

- 文案统一为「产出基准」：
  - `启用产出基准（覆盖酒馆预设 max_tokens）`
  - `产出基准（Token，320~2000）`
- 新增 `getOutputBaselineSystemMessage()`，在构建消息栈时注入一条**保守长度偏好** system：
  - 提示接近基准并允许区间波动；
  - 明确“保持文风/剧情一致，不新增设定，不总结，不改人称时序”。
- 注入点覆盖三条路径：
  - `EU_ST_NATIVE_CHAT_STACK` optional system
  - strict replica 分支
  - 非 native 的 sysCore 分支
- 状态条文案改为 `产出基准：...`，并标注“仅上限浮动 + 软引导，不做多轮补写”。
- 构建号更新为 `20260501-028`。

## 路径

- `public/eu-demo.html`

## 说明

- 本次没有恢复旧版 `ensureReplyMinTokens` 或强制续写；
- 若单条仍显著偏短，属于模型提前收束场景，不是多轮补长链路导致。

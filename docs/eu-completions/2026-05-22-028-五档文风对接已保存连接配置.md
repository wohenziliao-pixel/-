# 五档文风对接已保存连接配置（v115）

## 本机核查（`data/wohenziliao/settings.json`）

| 酒馆配置名 | api（来源） | model | 与名称是否一致 |
|------------|-------------|-------|----------------|
| **DEEPSEEKV3** | openrouter | deepseek/deepseek-chat-v3-0324 | ✅ |
| **GROK4.3** | xai | grok-4.3 | ✅ |
| **GLM5.1** | custom | z-ai/glm-5.1 | ✅（注意名是 **GLM5.1** 无空格） |
| **VENICE** | custom | venice-uncensored | ✅ |
| **Euryale 70B** | openrouter | sao10k/l3.1-euryale-70b | ✅ |

说明：Euryale 条目里 `api-url` 仍残留智谱地址，不影响 OpenRouter 生成（EU 按 api/model 走）。

## 文风 ↔ 酒馆配置（已写入 `eu.html`）

| EU 文风 | 酒馆配置名 `stTavernConnName` |
|---------|------------------------------|
| 现代风 | DEEPSEEKV3 |
| 文学风 | GROK4.3 |
| 温柔风 | GLM5.1 |
| 创意风 | VENICE |
| 重口风 | Euryale 70B |

## 代码

- `EU_WRITING_STYLE_PRESETS` 名称与模型与上表对齐（温柔风模型改为 `z-ai/glm-5.1`）。
- `euCacheTavernConnectionProfilesFromSettingsGet` + `applyEuTavernConnProfileToLive`：发消息时从 `/api/settings/get` 读取你保存的连接配置，**优先 `profile.model`** 覆盖生成参数。
- `dumpEuWritingStyleApiAudit()` 增加 `tavernFound` / `tavernApi` / `tavernModel` 字段便于核对。

## 自测

1. 硬刷新 `eu.html`。
2. 控制台：`dumpEuWritingStyleApiAudit()`，五条 `tavernFound` 应为 `true`。
3. 逐档切换文风发一条消息，Network 里 `chat_completion_source` + `model` 应随表变化。

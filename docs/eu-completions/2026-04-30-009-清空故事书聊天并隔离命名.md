# 2026-04-30-009 清空故事书聊天并隔离命名

## 任务
- 清除此前积存的故事书聊天残留（测试环境，含进行中会话）。
- 避免 EU 与酒馆同故事书会话继续混用，改为 EU 侧独立命名空间。

## 执行结果
- 已清空 `data/default-user/chats/` 下现有聊天目录（当前为 0 个目录）。

## 改动
- `public/eu-demo.html`
  - `startStorybookChat`：故事书会话名统一改为 `故事书·<标题>`，作为 EU 侧固定命名空间。
  - `ensureConversationForCharacter`：当会话名已是 `故事书·` 前缀时，不再走“前缀/非前缀互迁移”兜底，避免把旧的混用会话再次拉回。

## 影响路径
- `public/eu-demo.html`
- 运行时数据目录：`data/default-user/chats/`（清空）

## 用户自测
- 任意故事书新开会话后，列表应显示正常标题（UI 会隐藏 `故事书·` 前缀），但内部命名已隔离。
- 删除会话后，确认不会再复用旧的无前缀本地会话映射。

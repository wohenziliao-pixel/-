# 手动续写卡住 v109（SSE 尾包 / 上下文 / 流式展示）

## 任务

v108 下长对话点左下角「续写」仍可能：有点击日志、三点在动，但气泡末尾不接写。需在本机 `8000/eu.html` 排查 generate 流式/合并与 browser-state 拖慢，修到续写稳定接写。不改酒馆 `index.html` / `script.js`。

## 根因（v108 仍卡）

1. **SSE 尾包未解析**：`requestAiReplyStream` 在 `reader.done` 后未 `flush` 残留 `buffer`，最后一行无换行时整段 `data:` 被丢弃 → `full` 为空，体感一直三点。
2. **手动续写上下文挤爆**：仍注入 `buildCompactStorybookDirective`、全局/设定集 system，且 history 预算未给 3500 字摘录预留 → 长对话有效 history 过少或请求偏重。
3. **流式展示被 `mergeContinuationReply` 提前剥空**：模型首包与结尾摘录重叠时，合并逻辑在 paint 阶段就把 chunk 剥光，像「卡住」。
4. **续写前等 `settings/get`**：无缓存时阻塞在 `fetchStLiveGenerationProfile`，与 browser-state 大时叠加延迟。

## 做了什么

- **SSE**：`euConsumeSseLine` / `euFlushSseBuffer` / `euTryExtractReplyFromNonSseBody`；流结束 `decoder.finalize()` 并处理尾 buffer。
- **手动续写 prompt 栈**：`manualContinue` 时跳过 compactDirective、scoped/global/crossover system；`selectHistoryWithinContextBudget` 预留 560。
- **预设**：`fetchStLiveGenerationProfile({ allowStale: true })`；点击续写时后台预取；手动续写 `skipReplyPostProcess: true`。
- **流式**：续写专用 `AbortController` 超时中止 generate；`continue: chunk` 日志；流式 paint 用 `rawChunkDisplay` 先展示再接合并。
- **构建戳**：`20260522-manual-continue-fix-v109`。

## 改动路径

- `public/eu.html`

## 自测

1. Ctrl+Shift+R，`document.documentElement.dataset.euClientBuild` → `20260522-manual-continue-fix-v109`。
2. 长对话（20+ 轮）点续写：Console 依次 `continue: click` → `continue: request` → `continue: chunk` → `continue: done`；气泡原文末尾流式增长。
3. Network：`POST .../chat-completions/generate` 流式 body 结束后气泡有新增；若仅单包 JSON 也应能兜底解析。
4. Console 若 `browser-state large`：可用 `http://127.0.0.1:8000/eu-localstorage-port.html` 瘦身后再测（非代码必改项）。

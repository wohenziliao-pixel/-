# 文风转换恢复（v111）

## 任务

`2026-05-22-001` 将 `public/eu.html` 回退到仓库 HEAD 后，「文风转换」菜单、弹窗、持久化与 API 路由一并丢失。用户要求恢复文风设置按钮及相关逻辑。

## 实现

- **构建戳**：`20260522-writing-style-restore-v111`
- **菜单**：故事书对话底栏 ☰ →「文风转换」（在「新增规则」下方）
- **弹窗**：`#writing-style-modal`，五档（现代 / 文学 / 温柔 / 创意 / 重口），含 API 来源提示
- **预设**：`EU_WRITING_STYLE_PRESETS`（现代风 → OpenRouter `deepseek/deepseek-chat-v3-0324`；其余 → xAI `grok-4.3`）
- **持久化**：`readStorybookMetaBundle` / `writeStorybookMetaBundle` 的 `writingStyleId`；`hydrateAuthorRuleFromStorybookStore` 同步到 `metadata.authorRule`
- **生成**：`buildWritingStyleSystemMessage()` 注入 `optionalSystem`（虚拟恋人跳过）；`resolveEuLiveProfileForGeneration` + `fetchStLiveGenerationProfile` 覆盖 `chat_completion_source` / `model`；`buildStGeneratePayload` 补充 OpenRouter fallback 字段
- **状态条**：`updateWorldChatFeatureStatus` 显示当前文风与 API 来源
- **诊断**：控制台 `dumpEuWritingStyleApiAudit()`

## 改动路径

- `public/eu.html`
- `docs/eu-completions/2026-05-22-013-文风转换恢复v111.md`

## 自测建议

1. 强刷 `http://127.0.0.1:8000/eu.html`，控制台确认 `EU_CLIENT_BUILD_STAMP` 为 `20260522-writing-style-restore-v111`。
2. 进入故事书对话 → ☰ → 应有「文风转换」，弹窗五档可点选，Toast 与状态条更新。
3. 选「现代风」发一条消息，Network 中 `generate` 请求应为 `openrouter` + deepseek 模型。
4. 改「文学风」再发一条，应为 `xai` + `grok-4.3`（需站长/共用账号已配 `api_key_xai`）。
5. 刷新页面，文风应保持（localStorage 故事书 meta）。
6. 控制台执行 `dumpEuWritingStyleApiAudit()` 核对五档映射。

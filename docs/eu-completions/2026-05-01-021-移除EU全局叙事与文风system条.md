# 2026-05-01-021 移除 EU 全局叙事与文风 system 条

## 需求
- 删除酒馆中不存在的、对叙事/文风做要求的 EU 注入；保留用户显式配置与卡面事实数据。

## 已移除或弱化
- 删除 `EU_DEFAULT_NARRATIVE_AN` 及所有 `euNarrativeStyle` 注入（native / strict_replica / 旧预算路径、`buildStorybookRuntimeSystemMessages` 前置条与英文 guide）。
- 删除 `getRpLanguageSystemMessage` 及其在正则开启时的注入。
- `EU_DEGENERATE_RETRY_SYSTEM`：改为仅技术性「无效/过短则重生成」一句，不含叙事偏好。
- `regenerateDialogueOnlyIfNeeded`：去掉「仅对白正文」式表述，改为可见正文 + 禁元信息。
- `buildBudgetedMessages` 中 `buildCompactStorybookDirective` 外包层改为「上下文摘录（非文风指令）」。
- `toBackendSafeMessages` 开场折叠 system：去掉「须承接扮演」类文风句，仅作非用户语句标注。
- `getEuFeatureProbe.euDefaultNarrativeAnEnabled` 置为 `false`；`EU_ST_PARITY_NOTES` 与 `dumpEuStorybookPromptAudit` 字段同步。

## 仍可能影响文风、但属用户/卡面数据（未删）
- 故事书卡 `first_mes` / `scenario` / `post_history_instructions` / `mes_example`（经压缩摘录进入 directive）。
- 「新增规则」`getStorybookRuleSystemMessage`、乱入 `buildCrossoverSystemMessage`、全局设定集 `buildGlobalSettingsSystemMessage`、用户画像。
- `ensureReplyMinTokens` / 续写链路与 `sanitizeRpReply`（技术后处理，非「教怎么写小说」的独立文风条）。

## 自测
- 发消息前 `getEuAlignmentProbes().slice(-1)[0]`：`systemCount` 应不再因 euNarrativeStyle 恒 +1；对比移除前后请求体 system 块。

---
description: 自主推进任务；大规模删除前须确认
alwaysApply: true
---

# 协作方式（自主执行）

- **默认一口气完成**：接到任务后自主拆解、执行、自检并交付结果；不要为常规步骤逐步征求用户同意。
- **尽量不问**：信息不足时优先从代码库、配置与命令行自行查证；仅在确实无法安全推断时再提问。
- **大规模删除须先确认**：在执行可能造成广泛、不可逆破坏的操作前，必须先向用户说明范围并征得明确同意。包括但不限于：批量删除目录或大量文件、清空数据库/向量库、重置仓库、对共享/生产数据的破坏性变更。日常改动的单文件/小范围删除不在此列。
- **例外**：若用户当条消息明确要求「先确认再动手」，则按其指示执行。

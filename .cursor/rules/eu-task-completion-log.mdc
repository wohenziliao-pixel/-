---
description: 每完成 EU/本仓库相关任务时写入完成记录文件
alwaysApply: true
---

# EU 任务完成记录

- 每次在本仓库**执行并完成**一项可交付任务后（代码修改、行为变更、可复现的排查结论等），在 `docs/eu-completions/` **新建**一个文件，**不要**只往旧文件里追加为唯一出口。
- 命名：`YYYY-MM-DD-001-短标题.md`，同一日多条则递增序号。
- 文内写清：任务要做什么、做了什么、改动了哪些路径（可列表）、需要用户自测的项（如有）。
